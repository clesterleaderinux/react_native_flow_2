import React from 'react';
import { Fundamentals } from './src/components';

/**
 * Demo App showing React Native Fundamentals
 * 
 * To use this app, replace the content of App.tsx with:
 * 
 * import React from 'react';
 * import { Fundamentals } from './src/components';
 * 
 * const App: React.FC = () => {
 *   return <Fundamentals />;
 * };
 * 
 * export default App;
 */

const DemoApp: React.FC = () => {
  return <Fundamentals />;
};

export default DemoApp;